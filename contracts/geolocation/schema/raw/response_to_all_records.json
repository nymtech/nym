{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AllRecordsPagedResponse",
  "description": "A page of the global enumeration across both entry classes, and the input a client folds to recompute the digest for itself.",
  "type": "object",
  "required": [
    "records"
  ],
  "properties": {
    "records": {
      "description": "Records in ascending key order.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/GeolocationRecord"
      }
    },
    "start_next_after": {
      "description": "Cursor to pass as the next `start_after`, or `None` when the enumeration is exhausted.",
      "anyOf": [
        {
          "$ref": "#/definitions/RecordKey"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "additionalProperties": false,
  "definitions": {
    "Addr": {
      "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
      "type": "string"
    },
    "AgentPermissions": {
      "description": "What a whitelisted agent is permitted to write. The flags are independent: an agent may be trusted to measure without being trusted to relay self-declarations, or the reverse.",
      "type": "object",
      "required": [
        "can_measure",
        "can_relay_self_declared"
      ],
      "properties": {
        "can_measure": {
          "description": "May write [`Source::Measured`] entries.",
          "type": "boolean"
        },
        "can_relay_self_declared": {
          "description": "May write [`Source::SelfDeclared`] entries on a subject's behalf.",
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "Binary": {
      "description": "Binary is a wrapper around Vec<u8> to add base64 de/serialization with serde. It also adds some helper methods to help encode inline.\n\nThis is only needed as serde-json-{core,wasm} has a horrible encoding for Vec<u8>. See also <https://github.com/CosmWasm/cosmwasm/blob/main/docs/MESSAGE_TYPES.md>.",
      "type": "string"
    },
    "EntryKey": {
      "description": "Names one location entry: which subject, and which source asserted it. What a reader queries by and what [`crate::ExecuteMsg::RemoveEntries`] deletes by.\n\nA named pair rather than a tuple, because the two components are not interchangeable and the storage key's ordering depends on which is which.",
      "type": "object",
      "required": [
        "source",
        "subject"
      ],
      "properties": {
        "source": {
          "$ref": "#/definitions/Source"
        },
        "subject": {
          "$ref": "#/definitions/Subject"
        }
      },
      "additionalProperties": false
    },
    "GeolocationRecord": {
      "description": "A digest-committed record, together with the key it is stored under. Everything the accumulator folds is one of these, and [`GeolocationRecord::digest_leaf`] is the only place the canonical leaf encoding lives. The variants wrap the per-class record types rather than restating their fields, so each class is described in exactly one place. Serde's external tagging makes a newtype variant transparent, so this is the same JSON a struct variant would produce.",
      "oneOf": [
        {
          "description": "A location entry for one subject from one source.",
          "type": "object",
          "required": [
            "location"
          ],
          "properties": {
            "location": {
              "$ref": "#/definitions/LocationRecord"
            }
          },
          "additionalProperties": false
        },
        {
          "description": "One whitelisted agent and its permissions. Folded into the same digest as location entries, because measured entries carry no signature and a client verifying the digest would otherwise have no way to tell which writers were authorised.",
          "type": "object",
          "required": [
            "whitelisted_agent"
          ],
          "properties": {
            "whitelisted_agent": {
              "$ref": "#/definitions/WhitelistEntry"
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "LocationAttestation": {
      "description": "The subject's own attestation over a self-declared location, present only on [`Source::SelfDeclared`] entries.",
      "type": "object",
      "required": [
        "declared_at",
        "signature"
      ],
      "properties": {
        "declared_at": {
          "description": "Unix timestamp, in seconds, at which the subject signed the artifact. Strictly monotonic per subject and bounded above by block time plus `MAX_SKEW`, so a superseded artifact cannot be replayed and a far-future one cannot freeze the slot.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "signature": {
          "description": "The subject's ed25519 signature over the domain-separated signing payload.",
          "allOf": [
            {
              "$ref": "#/definitions/Binary"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "LocationEntry": {
      "description": "A stored location entry: the opaque payload, when it reached the chain, and the subject's attestation where one exists.",
      "type": "object",
      "required": [
        "checked_at",
        "payload"
      ],
      "properties": {
        "attestation": {
          "description": "The subject's attestation. Populated on [`Source::SelfDeclared`] entries only; measured and overridden entries carry no signature from anyone.",
          "anyOf": [
            {
              "$ref": "#/definitions/LocationAttestation"
            },
            {
              "type": "null"
            }
          ]
        },
        "checked_at": {
          "description": "Unix timestamp, in seconds, taken from the block that wrote this entry. Committed to the digest leaf, so re-submitting an unchanged location changes the digest and a client that verifies the digest also verifies freshness.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "payload": {
          "$ref": "#/definitions/LocationPayload"
        }
      },
      "additionalProperties": false
    },
    "LocationPayload": {
      "description": "The location payload as the contract holds it: a format version plus opaque `content` bytes the contract never parses, validates, normalises or re-serialises.\n\nUnder `version = 1` the `content` bytes are UTF-8 JSON, so a web consumer can base64-decode and `JSON.parse` without obtaining a schema. The version byte sits outside `content` so a later version can change the format itself, not merely the schema.\n\nStoring the bytes verbatim is a correctness invariant rather than an optimisation: a relayed self-declaration's signature is over exactly these bytes, and JSON key ordering, whitespace and floating-point formatting all vary between implementations.",
      "type": "object",
      "required": [
        "content",
        "version"
      ],
      "properties": {
        "content": {
          "description": "The opaque payload bytes, stored and returned exactly as submitted.",
          "allOf": [
            {
              "$ref": "#/definitions/Binary"
            }
          ]
        },
        "version": {
          "description": "Selects the format of `content`. A version is never reused for another format.",
          "type": "integer",
          "format": "uint8",
          "minimum": 0.0
        }
      },
      "additionalProperties": false
    },
    "LocationRecord": {
      "type": "object",
      "required": [
        "entry",
        "source",
        "subject"
      ],
      "properties": {
        "entry": {
          "$ref": "#/definitions/LocationEntry"
        },
        "source": {
          "$ref": "#/definitions/Source"
        },
        "subject": {
          "$ref": "#/definitions/Subject"
        }
      },
      "additionalProperties": false
    },
    "Method": {
      "description": "How a measurement was obtained. Closed enum for the same reason [`SubjectClass`] is: it lives in the key, so adding a vendor or a technique is one variant with no leaf-encoding change and no state migration.\n\nRetiring a variant is a migration that must subtract every affected leaf from the accumulator before deleting the entries, and its discriminant must be tombstoned rather than reused.",
      "oneOf": [
        {
          "description": "An ipinfo.io lookup of the subject's resolved addresses.",
          "type": "string",
          "enum": [
            "ip_info"
          ]
        }
      ]
    },
    "RecordKey": {
      "description": "The logical key of a digest-committed record: the [`crate::QueryMsg::AllRecords`] cursor, and the identity half of a [`GeolocationRecord`]. The on-chain storage key is a separate concern, handled by each store, so this type carries no storage-codec logic.\n\nDeliberately not what [`crate::ExecuteMsg::RemoveEntries`] accepts, even though it can name a location entry: it can also name a whitelist entry, and a whitelist entry must only be removed through [`crate::ExecuteMsg::RemoveWhitelistedAgent`], where the digest and the authorisation semantics are handled together.",
      "oneOf": [
        {
          "type": "object",
          "required": [
            "location"
          ],
          "properties": {
            "location": {
              "$ref": "#/definitions/EntryKey"
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "whitelisted_agent"
          ],
          "properties": {
            "whitelisted_agent": {
              "type": "object",
              "required": [
                "agent"
              ],
              "properties": {
                "agent": {
                  "$ref": "#/definitions/Addr"
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "Source": {
      "description": "Who asserted an entry, as the final component of its key. A single discriminant rather than separate `kind` and `writer` components, so combinations that would be meaningless are not representable.",
      "oneOf": [
        {
          "description": "A third-party measurement. Carries the measuring agent, so each authorised agent occupies its own slot and concurrent agents never overwrite one another.",
          "type": "object",
          "required": [
            "measured"
          ],
          "properties": {
            "measured": {
              "type": "object",
              "required": [
                "agent",
                "method"
              ],
              "properties": {
                "agent": {
                  "$ref": "#/definitions/Addr"
                },
                "method": {
                  "$ref": "#/definitions/Method"
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        },
        {
          "description": "The subject's own signed declaration, relayed by some agent. Carries no writer component, so a subject has exactly one self-declared slot no matter which agent relayed it; conflicting relays are resolved by `declared_at` monotonicity.",
          "type": "string",
          "enum": [
            "self_declared"
          ]
        },
        {
          "description": "An admin-set value. Names the admin role rather than an address, so transferring the role does not orphan existing overrides.",
          "type": "string",
          "enum": [
            "override"
          ]
        }
      ]
    },
    "Subject": {
      "description": "The subject an entry describes: its class plus the id whose encoding that class fixes. This typed form is what messages and query responses carry; [`Subject::id_bytes`] is the opaque per-class encoding used in the storage key and the digest leaf.",
      "oneOf": [
        {
          "description": "A bonded nym-node. The id encodes as a big-endian `u32`, so ids order numerically under the store's key ordering and decode back to a [`NodeId`] for the mixnet unbond callback.",
          "type": "object",
          "required": [
            "nym_node"
          ],
          "properties": {
            "nym_node": {
              "type": "object",
              "required": [
                "node_id"
              ],
              "properties": {
                "node_id": {
                  "type": "integer",
                  "format": "uint32",
                  "minimum": 0.0
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "WhitelistEntry": {
      "description": "One whitelisted agent together with its permissions.",
      "type": "object",
      "required": [
        "agent",
        "permissions"
      ],
      "properties": {
        "agent": {
          "$ref": "#/definitions/Addr"
        },
        "permissions": {
          "$ref": "#/definitions/AgentPermissions"
        }
      },
      "additionalProperties": false
    }
  }
}
