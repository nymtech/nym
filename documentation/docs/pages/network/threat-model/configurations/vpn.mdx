---
title: "Centralised VPN"
description: "A single VPN server hides the client IP and nothing else; the operator sees both ends."
schemaType: "TechArticle"
section: "Network"
lastUpdated: "2026-07-29"
---

import { GenericScenarioView } from 'components/threat-model/GenericScenarioView'
import { requireGenericScenario } from 'lib/privacy-model/examples/generic'

export const scenario = requireGenericScenario('vpn')

# Centralised VPN

<GenericScenarioView scenario={scenario} />
