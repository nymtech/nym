{
  "contract_name": "nym-geolocation-contract",
  "contract_version": "0.1.0",
  "idl_version": "1.0.0",
  "instantiate": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "InstantiateMsg",
    "description": "Instantiate the geolocation contract. The instantiator becomes the admin.",
    "type": "object",
    "required": [
      "initial_whitelist",
      "mixnet_contract_address"
    ],
    "properties": {
      "initial_whitelist": {
        "description": "Agents authorised to write from the outset. May be empty, in which case the admin whitelists them afterwards.",
        "type": "array",
        "items": {
          "$ref": "#/definitions/InitialAgent"
        }
      },
      "max_batch_size": {
        "type": [
          "integer",
          "null"
        ],
        "format": "uint32",
        "minimum": 0.0
      },
      "max_payload_size": {
        "type": [
          "integer",
          "null"
        ],
        "format": "uint32",
        "minimum": 0.0
      },
      "max_skew_secs": {
        "description": "Overrides for the contract's tunables; each falls back to its `DEFAULT_` constant.",
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "minimum": 0.0
      },
      "mixnet_contract_address": {
        "description": "Mixnet contract address, used to resolve node identity keys and to authorise the unbond callback.",
        "type": "string"
      }
    },
    "additionalProperties": false,
    "definitions": {
      "AgentPermissions": {
        "description": "What a whitelisted agent is permitted to write. The flags are independent: an agent may be trusted to measure without being trusted to relay self-declarations, or the reverse.",
        "type": "object",
        "required": [
          "can_measure",
          "can_relay_self_declared"
        ],
        "properties": {
          "can_measure": {
            "description": "May write [`Source::Measured`] entries.",
            "type": "boolean"
          },
          "can_relay_self_declared": {
            "description": "May write [`Source::SelfDeclared`] entries on a subject's behalf.",
            "type": "boolean"
          }
        },
        "additionalProperties": false
      },
      "InitialAgent": {
        "description": "An agent whitelisted at instantiation, with the permissions it starts with.",
        "type": "object",
        "required": [
          "agent",
          "permissions"
        ],
        "properties": {
          "agent": {
            "type": "string"
          },
          "permissions": {
            "$ref": "#/definitions/AgentPermissions"
          }
        },
        "additionalProperties": false
      }
    }
  },
  "execute": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ExecuteMsg",
    "oneOf": [
      {
        "description": "Submit a batch of measurements. Sender must be whitelisted with `can_measure`.\n\nAll or nothing: one rejected entry fails the whole transaction. Ordering does not matter, since the accumulator is commutative, and the same key may appear more than once, in which case the last write stands.",
        "type": "object",
        "required": [
          "submit_measurements"
        ],
        "properties": {
          "submit_measurements": {
            "type": "object",
            "required": [
              "measurements"
            ],
            "properties": {
              "measurements": {
                "type": "array",
                "items": {
                  "$ref": "#/definitions/Measurement"
                }
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "Relay a batch of node-signed self-declarations. Sender must be whitelisted with `can_relay_self_declared`.\n\nKept separate from [`Self::SubmitMeasurements`] rather than merged into one batch: a relay carries data the agent did not produce and whose signature it cannot fully pre-validate against contract state, so one bad artifact must not be able to fail an agent's whole measurement sweep.",
        "type": "object",
        "required": [
          "relay_self_declarations"
        ],
        "properties": {
          "relay_self_declarations": {
            "type": "object",
            "required": [
              "declarations"
            ],
            "properties": {
              "declarations": {
                "type": "array",
                "items": {
                  "$ref": "#/definitions/NymNodeLocation"
                }
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "Create or replace an override entry. Admin only.",
        "type": "object",
        "required": [
          "set_override"
        ],
        "properties": {
          "set_override": {
            "type": "object",
            "required": [
              "payload",
              "subject"
            ],
            "properties": {
              "payload": {
                "$ref": "#/definitions/LocationPayload"
              },
              "subject": {
                "$ref": "#/definitions/Subject"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "Delete an override entry. Admin only. Every other source for that subject is left untouched, so retracting an override does not wait on a re-measurement.",
        "type": "object",
        "required": [
          "remove_override"
        ],
        "properties": {
          "remove_override": {
            "type": "object",
            "required": [
              "subject"
            ],
            "properties": {
              "subject": {
                "$ref": "#/definitions/Subject"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "Add an agent to the whitelist, or change an existing agent's permissions. Admin only.",
        "type": "object",
        "required": [
          "set_whitelisted_agent"
        ],
        "properties": {
          "set_whitelisted_agent": {
            "type": "object",
            "required": [
              "agent",
              "permissions"
            ],
            "properties": {
              "agent": {
                "type": "string"
              },
              "permissions": {
                "$ref": "#/definitions/AgentPermissions"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "Remove an agent from the whitelist. Admin only.\n\nNon-destructive: the agent's entries stay in storage and in the digest, and a conforming client stops honouring them immediately, because authorisation is evaluated against the current whitelist at read time. [`Self::RemoveEntries`] cleans up afterwards, as hygiene rather than as the security control.",
        "type": "object",
        "required": [
          "remove_whitelisted_agent"
        ],
        "properties": {
          "remove_whitelisted_agent": {
            "type": "object",
            "required": [
              "agent"
            ],
            "properties": {
              "agent": {
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "Delete the named entries, folding each removal into the digest. Admin only.\n\nExplicit keys rather than a purge scoped to one agent. The agent sits inside the trailing `source` key component, so there is no index by agent and a scoped purge would have to scan the whole store on every page, one admin transaction at a time. The admin instead pages [`QueryMsg::AllRecords`] off-chain, decides exactly what should go and names it, so the cost is proportional to what is deleted rather than to what is stored.",
        "type": "object",
        "required": [
          "remove_entries"
        ],
        "properties": {
          "remove_entries": {
            "type": "object",
            "required": [
              "keys"
            ],
            "properties": {
              "keys": {
                "type": "array",
                "items": {
                  "$ref": "#/definitions/EntryKey"
                }
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "Transfer the admin role. Admin only. There is always exactly one admin.",
        "type": "object",
        "required": [
          "update_admin"
        ],
        "properties": {
          "update_admin": {
            "type": "object",
            "required": [
              "admin"
            ],
            "properties": {
              "admin": {
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "Change the contract's tunables. Admin only; omitted fields keep their current value.",
        "type": "object",
        "required": [
          "update_config"
        ],
        "properties": {
          "update_config": {
            "type": "object",
            "properties": {
              "max_batch_size": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint32",
                "minimum": 0.0
              },
              "max_payload_size": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint32",
                "minimum": 0.0
              },
              "max_skew_secs": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint64",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "Cross-contract callback from the mixnet contract when `node_id` unbonds; deletes every entry for that node across all sources. Sender must be the configured mixnet contract.",
        "type": "object",
        "required": [
          "on_nym_node_unbond"
        ],
        "properties": {
          "on_nym_node_unbond": {
            "type": "object",
            "required": [
              "node_id"
            ],
            "properties": {
              "node_id": {
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      }
    ],
    "definitions": {
      "Addr": {
        "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
        "type": "string"
      },
      "AgentPermissions": {
        "description": "What a whitelisted agent is permitted to write. The flags are independent: an agent may be trusted to measure without being trusted to relay self-declarations, or the reverse.",
        "type": "object",
        "required": [
          "can_measure",
          "can_relay_self_declared"
        ],
        "properties": {
          "can_measure": {
            "description": "May write [`Source::Measured`] entries.",
            "type": "boolean"
          },
          "can_relay_self_declared": {
            "description": "May write [`Source::SelfDeclared`] entries on a subject's behalf.",
            "type": "boolean"
          }
        },
        "additionalProperties": false
      },
      "Binary": {
        "description": "Binary is a wrapper around Vec<u8> to add base64 de/serialization with serde. It also adds some helper methods to help encode inline.\n\nThis is only needed as serde-json-{core,wasm} has a horrible encoding for Vec<u8>. See also <https://github.com/CosmWasm/cosmwasm/blob/main/docs/MESSAGE_TYPES.md>.",
        "type": "string"
      },
      "EntryKey": {
        "description": "Names one location entry: which subject, and which source asserted it. What a reader queries by and what [`crate::ExecuteMsg::RemoveEntries`] deletes by.\n\nA named pair rather than a tuple, because the two components are not interchangeable and the storage key's ordering depends on which is which.",
        "type": "object",
        "required": [
          "source",
          "subject"
        ],
        "properties": {
          "source": {
            "$ref": "#/definitions/Source"
          },
          "subject": {
            "$ref": "#/definitions/Subject"
          }
        },
        "additionalProperties": false
      },
      "LocationPayload": {
        "description": "The location payload as the contract holds it: a format version plus opaque `content` bytes the contract never parses, validates, normalises or re-serialises.\n\nUnder `version = 1` the `content` bytes are UTF-8 JSON, so a web consumer can base64-decode and `JSON.parse` without obtaining a schema. The version byte sits outside `content` so a later version can change the format itself, not merely the schema.\n\nStoring the bytes verbatim is a correctness invariant rather than an optimisation: a relayed self-declaration's signature is over exactly these bytes, and JSON key ordering, whitespace and floating-point formatting all vary between implementations.",
        "type": "object",
        "required": [
          "content",
          "version"
        ],
        "properties": {
          "content": {
            "description": "The opaque payload bytes, stored and returned exactly as submitted.",
            "allOf": [
              {
                "$ref": "#/definitions/Binary"
              }
            ]
          },
          "version": {
            "description": "Selects the format of `content`. A version is never reused for another format.",
            "type": "integer",
            "format": "uint8",
            "minimum": 0.0
          }
        },
        "additionalProperties": false
      },
      "Measurement": {
        "description": "One measurement in a batch. The measuring agent is the message sender rather than a field, so an agent cannot write under another agent's key.",
        "type": "object",
        "required": [
          "method",
          "payload",
          "subject"
        ],
        "properties": {
          "method": {
            "$ref": "#/definitions/Method"
          },
          "payload": {
            "$ref": "#/definitions/LocationPayload"
          },
          "subject": {
            "$ref": "#/definitions/Subject"
          }
        },
        "additionalProperties": false
      },
      "Method": {
        "description": "How a measurement was obtained. Closed enum for the same reason [`SubjectClass`] is: it lives in the key, so adding a vendor or a technique is one variant with no leaf-encoding change and no state migration.\n\nRetiring a variant is a migration that must subtract every affected leaf from the accumulator before deleting the entries, and its discriminant must be tombstoned rather than reused.",
        "oneOf": [
          {
            "description": "An ipinfo.io lookup of the subject's resolved addresses.",
            "type": "string",
            "enum": [
              "ip_info"
            ]
          }
        ]
      },
      "NymNodeLocation": {
        "description": "A node's signed self-declaration of its own location: the artifact the node serves over HTTP, an agent relays verbatim, and the contract verifies.\n\nIt carries the encoded [`LocationPayload`] rather than a typed location, which is the whole point: the signature is over these bytes, and any component that parsed and re-emitted them could change field ordering or float formatting and silently break verification. Nothing on this path decodes the payload, so this type needs no `Location` and stays free of the `payload` feature, which is also what lets the contract use it.",
        "type": "object",
        "required": [
          "declared_at",
          "node_id",
          "payload",
          "signature"
        ],
        "properties": {
          "declared_at": {
            "description": "Unix timestamp, in seconds, at which the node signed. Strictly monotonic per node and bounded above by block time plus `MAX_SKEW`.",
            "type": "integer",
            "format": "uint64",
            "minimum": 0.0
          },
          "node_id": {
            "type": "integer",
            "format": "uint32",
            "minimum": 0.0
          },
          "payload": {
            "$ref": "#/definitions/LocationPayload"
          },
          "signature": {
            "description": "The node's ed25519 signature over [`Self::signing_payload`].",
            "allOf": [
              {
                "$ref": "#/definitions/Binary"
              }
            ]
          }
        },
        "additionalProperties": false
      },
      "Source": {
        "description": "Who asserted an entry, as the final component of its key. A single discriminant rather than separate `kind` and `writer` components, so combinations that would be meaningless are not representable.",
        "oneOf": [
          {
            "description": "A third-party measurement. Carries the measuring agent, so each authorised agent occupies its own slot and concurrent agents never overwrite one another.",
            "type": "object",
            "required": [
              "measured"
            ],
            "properties": {
              "measured": {
                "type": "object",
                "required": [
                  "agent",
                  "method"
                ],
                "properties": {
                  "agent": {
                    "$ref": "#/definitions/Addr"
                  },
                  "method": {
                    "$ref": "#/definitions/Method"
                  }
                },
                "additionalProperties": false
              }
            },
            "additionalProperties": false
          },
          {
            "description": "The subject's own signed declaration, relayed by some agent. Carries no writer component, so a subject has exactly one self-declared slot no matter which agent relayed it; conflicting relays are resolved by `declared_at` monotonicity.",
            "type": "string",
            "enum": [
              "self_declared"
            ]
          },
          {
            "description": "An admin-set value. Names the admin role rather than an address, so transferring the role does not orphan existing overrides.",
            "type": "string",
            "enum": [
              "override"
            ]
          }
        ]
      },
      "Subject": {
        "description": "The subject an entry describes: its class plus the id whose encoding that class fixes. This typed form is what messages and query responses carry; [`Subject::id_bytes`] is the opaque per-class encoding used in the storage key and the digest leaf.",
        "oneOf": [
          {
            "description": "A bonded nym-node. The id encodes as a big-endian `u32`, so ids order numerically under the store's key ordering and decode back to a [`NodeId`] for the mixnet unbond callback.",
            "type": "object",
            "required": [
              "nym_node"
            ],
            "properties": {
              "nym_node": {
                "type": "object",
                "required": [
                  "node_id"
                ],
                "properties": {
                  "node_id": {
                    "type": "integer",
                    "format": "uint32",
                    "minimum": 0.0
                  }
                },
                "additionalProperties": false
              }
            },
            "additionalProperties": false
          }
        ]
      }
    }
  },
  "query": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "QueryMsg",
    "oneOf": [
      {
        "type": "object",
        "required": [
          "admin"
        ],
        "properties": {
          "admin": {
            "type": "object",
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": [
          "config"
        ],
        "properties": {
          "config": {
            "type": "object",
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "A single entry, by its full key.",
        "type": "object",
        "required": [
          "entry"
        ],
        "properties": {
          "entry": {
            "type": "object",
            "required": [
              "source",
              "subject"
            ],
            "properties": {
              "source": {
                "$ref": "#/definitions/Source"
              },
              "subject": {
                "$ref": "#/definitions/Subject"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "Every entry for one subject, across all sources. Unpaginated: a subject holds at most one entry per agent plus a self-declaration and an override.",
        "type": "object",
        "required": [
          "subject_entries"
        ],
        "properties": {
          "subject_entries": {
            "type": "object",
            "required": [
              "subject"
            ],
            "properties": {
              "subject": {
                "$ref": "#/definitions/Subject"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "Every entry for one bonded nym-node. The common case of [`Self::SubjectEntries`], saving callers that work in `NodeId`s from assembling a [`Subject`] themselves.",
        "type": "object",
        "required": [
          "nym_node_entries"
        ],
        "properties": {
          "nym_node_entries": {
            "type": "object",
            "required": [
              "node_id"
            ],
            "properties": {
              "node_id": {
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "Only the measured entries for one subject, dropping any self-declaration and override.",
        "type": "object",
        "required": [
          "subject_measurements"
        ],
        "properties": {
          "subject_measurements": {
            "type": "object",
            "required": [
              "subject"
            ],
            "properties": {
              "subject": {
                "$ref": "#/definitions/Subject"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "Paginated enumeration of every digest-committed record, across both entry classes. This is the global pull a client folds to recompute and verify the digest, so it has to cover the agent whitelist as well as the location entries.",
        "type": "object",
        "required": [
          "all_records"
        ],
        "properties": {
          "all_records": {
            "type": "object",
            "properties": {
              "limit": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint32",
                "minimum": 0.0
              },
              "start_after": {
                "anyOf": [
                  {
                    "$ref": "#/definitions/RecordKey"
                  },
                  {
                    "type": "null"
                  }
                ]
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "The 32-byte collapse of the accumulator. A convenience for comparing digests; it carries no proof, since smart queries cannot. Proving requires a raw store read at the documented digest key.",
        "type": "object",
        "required": [
          "digest"
        ],
        "properties": {
          "digest": {
            "type": "object",
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "The full agent whitelist. Unpaginated: the set is small and NYM-controlled.",
        "type": "object",
        "required": [
          "whitelist"
        ],
        "properties": {
          "whitelist": {
            "type": "object",
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      }
    ],
    "definitions": {
      "Addr": {
        "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
        "type": "string"
      },
      "EntryKey": {
        "description": "Names one location entry: which subject, and which source asserted it. What a reader queries by and what [`crate::ExecuteMsg::RemoveEntries`] deletes by.\n\nA named pair rather than a tuple, because the two components are not interchangeable and the storage key's ordering depends on which is which.",
        "type": "object",
        "required": [
          "source",
          "subject"
        ],
        "properties": {
          "source": {
            "$ref": "#/definitions/Source"
          },
          "subject": {
            "$ref": "#/definitions/Subject"
          }
        },
        "additionalProperties": false
      },
      "Method": {
        "description": "How a measurement was obtained. Closed enum for the same reason [`SubjectClass`] is: it lives in the key, so adding a vendor or a technique is one variant with no leaf-encoding change and no state migration.\n\nRetiring a variant is a migration that must subtract every affected leaf from the accumulator before deleting the entries, and its discriminant must be tombstoned rather than reused.",
        "oneOf": [
          {
            "description": "An ipinfo.io lookup of the subject's resolved addresses.",
            "type": "string",
            "enum": [
              "ip_info"
            ]
          }
        ]
      },
      "RecordKey": {
        "description": "The logical key of a digest-committed record: the [`crate::QueryMsg::AllRecords`] cursor, and the identity half of a [`GeolocationRecord`]. The on-chain storage key is a separate concern, handled by each store, so this type carries no storage-codec logic.\n\nDeliberately not what [`crate::ExecuteMsg::RemoveEntries`] accepts, even though it can name a location entry: it can also name a whitelist entry, and a whitelist entry must only be removed through [`crate::ExecuteMsg::RemoveWhitelistedAgent`], where the digest and the authorisation semantics are handled together.",
        "oneOf": [
          {
            "type": "object",
            "required": [
              "location"
            ],
            "properties": {
              "location": {
                "$ref": "#/definitions/EntryKey"
              }
            },
            "additionalProperties": false
          },
          {
            "type": "object",
            "required": [
              "whitelisted_agent"
            ],
            "properties": {
              "whitelisted_agent": {
                "type": "object",
                "required": [
                  "agent"
                ],
                "properties": {
                  "agent": {
                    "$ref": "#/definitions/Addr"
                  }
                },
                "additionalProperties": false
              }
            },
            "additionalProperties": false
          }
        ]
      },
      "Source": {
        "description": "Who asserted an entry, as the final component of its key. A single discriminant rather than separate `kind` and `writer` components, so combinations that would be meaningless are not representable.",
        "oneOf": [
          {
            "description": "A third-party measurement. Carries the measuring agent, so each authorised agent occupies its own slot and concurrent agents never overwrite one another.",
            "type": "object",
            "required": [
              "measured"
            ],
            "properties": {
              "measured": {
                "type": "object",
                "required": [
                  "agent",
                  "method"
                ],
                "properties": {
                  "agent": {
                    "$ref": "#/definitions/Addr"
                  },
                  "method": {
                    "$ref": "#/definitions/Method"
                  }
                },
                "additionalProperties": false
              }
            },
            "additionalProperties": false
          },
          {
            "description": "The subject's own signed declaration, relayed by some agent. Carries no writer component, so a subject has exactly one self-declared slot no matter which agent relayed it; conflicting relays are resolved by `declared_at` monotonicity.",
            "type": "string",
            "enum": [
              "self_declared"
            ]
          },
          {
            "description": "An admin-set value. Names the admin role rather than an address, so transferring the role does not orphan existing overrides.",
            "type": "string",
            "enum": [
              "override"
            ]
          }
        ]
      },
      "Subject": {
        "description": "The subject an entry describes: its class plus the id whose encoding that class fixes. This typed form is what messages and query responses carry; [`Subject::id_bytes`] is the opaque per-class encoding used in the storage key and the digest leaf.",
        "oneOf": [
          {
            "description": "A bonded nym-node. The id encodes as a big-endian `u32`, so ids order numerically under the store's key ordering and decode back to a [`NodeId`] for the mixnet unbond callback.",
            "type": "object",
            "required": [
              "nym_node"
            ],
            "properties": {
              "nym_node": {
                "type": "object",
                "required": [
                  "node_id"
                ],
                "properties": {
                  "node_id": {
                    "type": "integer",
                    "format": "uint32",
                    "minimum": 0.0
                  }
                },
                "additionalProperties": false
              }
            },
            "additionalProperties": false
          }
        ]
      }
    }
  },
  "migrate": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "MigrateMsg",
    "description": "Message passed to the contract's `migrate` entry point.",
    "type": "object",
    "additionalProperties": false
  },
  "sudo": null,
  "responses": {
    "admin": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AdminResponse",
      "description": "Returned from Admin.query_admin()",
      "type": "object",
      "properties": {
        "admin": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false
    },
    "all_records": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AllRecordsPagedResponse",
      "description": "A page of the global enumeration across both entry classes, and the input a client folds to recompute the digest for itself.",
      "type": "object",
      "required": [
        "records"
      ],
      "properties": {
        "records": {
          "description": "Records in ascending key order.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/GeolocationRecord"
          }
        },
        "start_next_after": {
          "description": "Cursor to pass as the next `start_after`, or `None` when the enumeration is exhausted.",
          "anyOf": [
            {
              "$ref": "#/definitions/RecordKey"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "AgentPermissions": {
          "description": "What a whitelisted agent is permitted to write. The flags are independent: an agent may be trusted to measure without being trusted to relay self-declarations, or the reverse.",
          "type": "object",
          "required": [
            "can_measure",
            "can_relay_self_declared"
          ],
          "properties": {
            "can_measure": {
              "description": "May write [`Source::Measured`] entries.",
              "type": "boolean"
            },
            "can_relay_self_declared": {
              "description": "May write [`Source::SelfDeclared`] entries on a subject's behalf.",
              "type": "boolean"
            }
          },
          "additionalProperties": false
        },
        "Binary": {
          "description": "Binary is a wrapper around Vec<u8> to add base64 de/serialization with serde. It also adds some helper methods to help encode inline.\n\nThis is only needed as serde-json-{core,wasm} has a horrible encoding for Vec<u8>. See also <https://github.com/CosmWasm/cosmwasm/blob/main/docs/MESSAGE_TYPES.md>.",
          "type": "string"
        },
        "EntryKey": {
          "description": "Names one location entry: which subject, and which source asserted it. What a reader queries by and what [`crate::ExecuteMsg::RemoveEntries`] deletes by.\n\nA named pair rather than a tuple, because the two components are not interchangeable and the storage key's ordering depends on which is which.",
          "type": "object",
          "required": [
            "source",
            "subject"
          ],
          "properties": {
            "source": {
              "$ref": "#/definitions/Source"
            },
            "subject": {
              "$ref": "#/definitions/Subject"
            }
          },
          "additionalProperties": false
        },
        "GeolocationRecord": {
          "description": "A digest-committed record, together with the key it is stored under. Everything the accumulator folds is one of these, and [`GeolocationRecord::digest_leaf`] is the only place the canonical leaf encoding lives. The variants wrap the per-class record types rather than restating their fields, so each class is described in exactly one place. Serde's external tagging makes a newtype variant transparent, so this is the same JSON a struct variant would produce.",
          "oneOf": [
            {
              "description": "A location entry for one subject from one source.",
              "type": "object",
              "required": [
                "location"
              ],
              "properties": {
                "location": {
                  "$ref": "#/definitions/LocationRecord"
                }
              },
              "additionalProperties": false
            },
            {
              "description": "One whitelisted agent and its permissions. Folded into the same digest as location entries, because measured entries carry no signature and a client verifying the digest would otherwise have no way to tell which writers were authorised.",
              "type": "object",
              "required": [
                "whitelisted_agent"
              ],
              "properties": {
                "whitelisted_agent": {
                  "$ref": "#/definitions/WhitelistEntry"
                }
              },
              "additionalProperties": false
            }
          ]
        },
        "LocationAttestation": {
          "description": "The subject's own attestation over a self-declared location, present only on [`Source::SelfDeclared`] entries.",
          "type": "object",
          "required": [
            "declared_at",
            "signature"
          ],
          "properties": {
            "declared_at": {
              "description": "Unix timestamp, in seconds, at which the subject signed the artifact. Strictly monotonic per subject and bounded above by block time plus `MAX_SKEW`, so a superseded artifact cannot be replayed and a far-future one cannot freeze the slot.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "signature": {
              "description": "The subject's ed25519 signature over the domain-separated signing payload.",
              "allOf": [
                {
                  "$ref": "#/definitions/Binary"
                }
              ]
            }
          },
          "additionalProperties": false
        },
        "LocationEntry": {
          "description": "A stored location entry: the opaque payload, when it reached the chain, and the subject's attestation where one exists.",
          "type": "object",
          "required": [
            "checked_at",
            "payload"
          ],
          "properties": {
            "attestation": {
              "description": "The subject's attestation. Populated on [`Source::SelfDeclared`] entries only; measured and overridden entries carry no signature from anyone.",
              "anyOf": [
                {
                  "$ref": "#/definitions/LocationAttestation"
                },
                {
                  "type": "null"
                }
              ]
            },
            "checked_at": {
              "description": "Unix timestamp, in seconds, taken from the block that wrote this entry. Committed to the digest leaf, so re-submitting an unchanged location changes the digest and a client that verifies the digest also verifies freshness.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "payload": {
              "$ref": "#/definitions/LocationPayload"
            }
          },
          "additionalProperties": false
        },
        "LocationPayload": {
          "description": "The location payload as the contract holds it: a format version plus opaque `content` bytes the contract never parses, validates, normalises or re-serialises.\n\nUnder `version = 1` the `content` bytes are UTF-8 JSON, so a web consumer can base64-decode and `JSON.parse` without obtaining a schema. The version byte sits outside `content` so a later version can change the format itself, not merely the schema.\n\nStoring the bytes verbatim is a correctness invariant rather than an optimisation: a relayed self-declaration's signature is over exactly these bytes, and JSON key ordering, whitespace and floating-point formatting all vary between implementations.",
          "type": "object",
          "required": [
            "content",
            "version"
          ],
          "properties": {
            "content": {
              "description": "The opaque payload bytes, stored and returned exactly as submitted.",
              "allOf": [
                {
                  "$ref": "#/definitions/Binary"
                }
              ]
            },
            "version": {
              "description": "Selects the format of `content`. A version is never reused for another format.",
              "type": "integer",
              "format": "uint8",
              "minimum": 0.0
            }
          },
          "additionalProperties": false
        },
        "LocationRecord": {
          "type": "object",
          "required": [
            "entry",
            "source",
            "subject"
          ],
          "properties": {
            "entry": {
              "$ref": "#/definitions/LocationEntry"
            },
            "source": {
              "$ref": "#/definitions/Source"
            },
            "subject": {
              "$ref": "#/definitions/Subject"
            }
          },
          "additionalProperties": false
        },
        "Method": {
          "description": "How a measurement was obtained. Closed enum for the same reason [`SubjectClass`] is: it lives in the key, so adding a vendor or a technique is one variant with no leaf-encoding change and no state migration.\n\nRetiring a variant is a migration that must subtract every affected leaf from the accumulator before deleting the entries, and its discriminant must be tombstoned rather than reused.",
          "oneOf": [
            {
              "description": "An ipinfo.io lookup of the subject's resolved addresses.",
              "type": "string",
              "enum": [
                "ip_info"
              ]
            }
          ]
        },
        "RecordKey": {
          "description": "The logical key of a digest-committed record: the [`crate::QueryMsg::AllRecords`] cursor, and the identity half of a [`GeolocationRecord`]. The on-chain storage key is a separate concern, handled by each store, so this type carries no storage-codec logic.\n\nDeliberately not what [`crate::ExecuteMsg::RemoveEntries`] accepts, even though it can name a location entry: it can also name a whitelist entry, and a whitelist entry must only be removed through [`crate::ExecuteMsg::RemoveWhitelistedAgent`], where the digest and the authorisation semantics are handled together.",
          "oneOf": [
            {
              "type": "object",
              "required": [
                "location"
              ],
              "properties": {
                "location": {
                  "$ref": "#/definitions/EntryKey"
                }
              },
              "additionalProperties": false
            },
            {
              "type": "object",
              "required": [
                "whitelisted_agent"
              ],
              "properties": {
                "whitelisted_agent": {
                  "type": "object",
                  "required": [
                    "agent"
                  ],
                  "properties": {
                    "agent": {
                      "$ref": "#/definitions/Addr"
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            }
          ]
        },
        "Source": {
          "description": "Who asserted an entry, as the final component of its key. A single discriminant rather than separate `kind` and `writer` components, so combinations that would be meaningless are not representable.",
          "oneOf": [
            {
              "description": "A third-party measurement. Carries the measuring agent, so each authorised agent occupies its own slot and concurrent agents never overwrite one another.",
              "type": "object",
              "required": [
                "measured"
              ],
              "properties": {
                "measured": {
                  "type": "object",
                  "required": [
                    "agent",
                    "method"
                  ],
                  "properties": {
                    "agent": {
                      "$ref": "#/definitions/Addr"
                    },
                    "method": {
                      "$ref": "#/definitions/Method"
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            },
            {
              "description": "The subject's own signed declaration, relayed by some agent. Carries no writer component, so a subject has exactly one self-declared slot no matter which agent relayed it; conflicting relays are resolved by `declared_at` monotonicity.",
              "type": "string",
              "enum": [
                "self_declared"
              ]
            },
            {
              "description": "An admin-set value. Names the admin role rather than an address, so transferring the role does not orphan existing overrides.",
              "type": "string",
              "enum": [
                "override"
              ]
            }
          ]
        },
        "Subject": {
          "description": "The subject an entry describes: its class plus the id whose encoding that class fixes. This typed form is what messages and query responses carry; [`Subject::id_bytes`] is the opaque per-class encoding used in the storage key and the digest leaf.",
          "oneOf": [
            {
              "description": "A bonded nym-node. The id encodes as a big-endian `u32`, so ids order numerically under the store's key ordering and decode back to a [`NodeId`] for the mixnet unbond callback.",
              "type": "object",
              "required": [
                "nym_node"
              ],
              "properties": {
                "nym_node": {
                  "type": "object",
                  "required": [
                    "node_id"
                  ],
                  "properties": {
                    "node_id": {
                      "type": "integer",
                      "format": "uint32",
                      "minimum": 0.0
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            }
          ]
        },
        "WhitelistEntry": {
          "description": "One whitelisted agent together with its permissions.",
          "type": "object",
          "required": [
            "agent",
            "permissions"
          ],
          "properties": {
            "agent": {
              "$ref": "#/definitions/Addr"
            },
            "permissions": {
              "$ref": "#/definitions/AgentPermissions"
            }
          },
          "additionalProperties": false
        }
      }
    },
    "config": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ConfigResponse",
      "description": "Response for [`crate::QueryMsg::Config`]: the mutable tunables together with the mixnet contract address, which is fixed at instantiation and reported here so a client can see which deployment this contract resolves node identity keys against.",
      "type": "object",
      "required": [
        "config",
        "mixnet_contract_address"
      ],
      "properties": {
        "config": {
          "$ref": "#/definitions/ContractConfig"
        },
        "mixnet_contract_address": {
          "$ref": "#/definitions/Addr"
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "ContractConfig": {
          "description": "The contract's tunables, all admin-adjustable. Held in state rather than as constants because each of them can need to move without a redeploy: a payload version may want more room or less, gas costs shift, and clock tolerance is an operational judgement.\n\nDeliberately cannot express the mixnet contract address, which is set once at instantiation and never changes. Keeping it out of this type means [`crate::ExecuteMsg::UpdateConfig`] has no way to reach it.",
          "type": "object",
          "required": [
            "max_batch_size",
            "max_payload_size",
            "max_skew_secs"
          ],
          "properties": {
            "max_batch_size": {
              "description": "Maximum entries in one batch.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "max_payload_size": {
              "description": "Maximum length of a payload's `content`, in bytes.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "max_skew_secs": {
              "description": "How far ahead of block time a `declared_at` may be, in seconds.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            }
          },
          "additionalProperties": false
        }
      }
    },
    "digest": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "DigestResponse",
      "description": "The 32-byte collapse of the LtHash accumulator. Unproven: smart queries carry no proof, so a client that needs one performs a raw store read at the digest key instead.",
      "type": "object",
      "required": [
        "digest"
      ],
      "properties": {
        "digest": {
          "$ref": "#/definitions/Binary"
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Binary": {
          "description": "Binary is a wrapper around Vec<u8> to add base64 de/serialization with serde. It also adds some helper methods to help encode inline.\n\nThis is only needed as serde-json-{core,wasm} has a horrible encoding for Vec<u8>. See also <https://github.com/CosmWasm/cosmwasm/blob/main/docs/MESSAGE_TYPES.md>.",
          "type": "string"
        }
      }
    },
    "entry": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "EntryResponse",
      "description": "Response for [`crate::QueryMsg::Entry`]; `None` if the slot is empty.",
      "type": "object",
      "properties": {
        "entry": {
          "anyOf": [
            {
              "$ref": "#/definitions/LocationEntry"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Binary": {
          "description": "Binary is a wrapper around Vec<u8> to add base64 de/serialization with serde. It also adds some helper methods to help encode inline.\n\nThis is only needed as serde-json-{core,wasm} has a horrible encoding for Vec<u8>. See also <https://github.com/CosmWasm/cosmwasm/blob/main/docs/MESSAGE_TYPES.md>.",
          "type": "string"
        },
        "LocationAttestation": {
          "description": "The subject's own attestation over a self-declared location, present only on [`Source::SelfDeclared`] entries.",
          "type": "object",
          "required": [
            "declared_at",
            "signature"
          ],
          "properties": {
            "declared_at": {
              "description": "Unix timestamp, in seconds, at which the subject signed the artifact. Strictly monotonic per subject and bounded above by block time plus `MAX_SKEW`, so a superseded artifact cannot be replayed and a far-future one cannot freeze the slot.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "signature": {
              "description": "The subject's ed25519 signature over the domain-separated signing payload.",
              "allOf": [
                {
                  "$ref": "#/definitions/Binary"
                }
              ]
            }
          },
          "additionalProperties": false
        },
        "LocationEntry": {
          "description": "A stored location entry: the opaque payload, when it reached the chain, and the subject's attestation where one exists.",
          "type": "object",
          "required": [
            "checked_at",
            "payload"
          ],
          "properties": {
            "attestation": {
              "description": "The subject's attestation. Populated on [`Source::SelfDeclared`] entries only; measured and overridden entries carry no signature from anyone.",
              "anyOf": [
                {
                  "$ref": "#/definitions/LocationAttestation"
                },
                {
                  "type": "null"
                }
              ]
            },
            "checked_at": {
              "description": "Unix timestamp, in seconds, taken from the block that wrote this entry. Committed to the digest leaf, so re-submitting an unchanged location changes the digest and a client that verifies the digest also verifies freshness.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "payload": {
              "$ref": "#/definitions/LocationPayload"
            }
          },
          "additionalProperties": false
        },
        "LocationPayload": {
          "description": "The location payload as the contract holds it: a format version plus opaque `content` bytes the contract never parses, validates, normalises or re-serialises.\n\nUnder `version = 1` the `content` bytes are UTF-8 JSON, so a web consumer can base64-decode and `JSON.parse` without obtaining a schema. The version byte sits outside `content` so a later version can change the format itself, not merely the schema.\n\nStoring the bytes verbatim is a correctness invariant rather than an optimisation: a relayed self-declaration's signature is over exactly these bytes, and JSON key ordering, whitespace and floating-point formatting all vary between implementations.",
          "type": "object",
          "required": [
            "content",
            "version"
          ],
          "properties": {
            "content": {
              "description": "The opaque payload bytes, stored and returned exactly as submitted.",
              "allOf": [
                {
                  "$ref": "#/definitions/Binary"
                }
              ]
            },
            "version": {
              "description": "Selects the format of `content`. A version is never reused for another format.",
              "type": "integer",
              "format": "uint8",
              "minimum": 0.0
            }
          },
          "additionalProperties": false
        }
      }
    },
    "nym_node_entries": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SubjectEntriesResponse",
      "description": "Response for [`crate::QueryMsg::SubjectEntries`] and its siblings: everything held for one subject, in ascending source order.",
      "type": "object",
      "required": [
        "entries",
        "subject"
      ],
      "properties": {
        "entries": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/SourceEntry"
          }
        },
        "subject": {
          "$ref": "#/definitions/Subject"
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "Binary": {
          "description": "Binary is a wrapper around Vec<u8> to add base64 de/serialization with serde. It also adds some helper methods to help encode inline.\n\nThis is only needed as serde-json-{core,wasm} has a horrible encoding for Vec<u8>. See also <https://github.com/CosmWasm/cosmwasm/blob/main/docs/MESSAGE_TYPES.md>.",
          "type": "string"
        },
        "LocationAttestation": {
          "description": "The subject's own attestation over a self-declared location, present only on [`Source::SelfDeclared`] entries.",
          "type": "object",
          "required": [
            "declared_at",
            "signature"
          ],
          "properties": {
            "declared_at": {
              "description": "Unix timestamp, in seconds, at which the subject signed the artifact. Strictly monotonic per subject and bounded above by block time plus `MAX_SKEW`, so a superseded artifact cannot be replayed and a far-future one cannot freeze the slot.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "signature": {
              "description": "The subject's ed25519 signature over the domain-separated signing payload.",
              "allOf": [
                {
                  "$ref": "#/definitions/Binary"
                }
              ]
            }
          },
          "additionalProperties": false
        },
        "LocationEntry": {
          "description": "A stored location entry: the opaque payload, when it reached the chain, and the subject's attestation where one exists.",
          "type": "object",
          "required": [
            "checked_at",
            "payload"
          ],
          "properties": {
            "attestation": {
              "description": "The subject's attestation. Populated on [`Source::SelfDeclared`] entries only; measured and overridden entries carry no signature from anyone.",
              "anyOf": [
                {
                  "$ref": "#/definitions/LocationAttestation"
                },
                {
                  "type": "null"
                }
              ]
            },
            "checked_at": {
              "description": "Unix timestamp, in seconds, taken from the block that wrote this entry. Committed to the digest leaf, so re-submitting an unchanged location changes the digest and a client that verifies the digest also verifies freshness.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "payload": {
              "$ref": "#/definitions/LocationPayload"
            }
          },
          "additionalProperties": false
        },
        "LocationPayload": {
          "description": "The location payload as the contract holds it: a format version plus opaque `content` bytes the contract never parses, validates, normalises or re-serialises.\n\nUnder `version = 1` the `content` bytes are UTF-8 JSON, so a web consumer can base64-decode and `JSON.parse` without obtaining a schema. The version byte sits outside `content` so a later version can change the format itself, not merely the schema.\n\nStoring the bytes verbatim is a correctness invariant rather than an optimisation: a relayed self-declaration's signature is over exactly these bytes, and JSON key ordering, whitespace and floating-point formatting all vary between implementations.",
          "type": "object",
          "required": [
            "content",
            "version"
          ],
          "properties": {
            "content": {
              "description": "The opaque payload bytes, stored and returned exactly as submitted.",
              "allOf": [
                {
                  "$ref": "#/definitions/Binary"
                }
              ]
            },
            "version": {
              "description": "Selects the format of `content`. A version is never reused for another format.",
              "type": "integer",
              "format": "uint8",
              "minimum": 0.0
            }
          },
          "additionalProperties": false
        },
        "Method": {
          "description": "How a measurement was obtained. Closed enum for the same reason [`SubjectClass`] is: it lives in the key, so adding a vendor or a technique is one variant with no leaf-encoding change and no state migration.\n\nRetiring a variant is a migration that must subtract every affected leaf from the accumulator before deleting the entries, and its discriminant must be tombstoned rather than reused.",
          "oneOf": [
            {
              "description": "An ipinfo.io lookup of the subject's resolved addresses.",
              "type": "string",
              "enum": [
                "ip_info"
              ]
            }
          ]
        },
        "Source": {
          "description": "Who asserted an entry, as the final component of its key. A single discriminant rather than separate `kind` and `writer` components, so combinations that would be meaningless are not representable.",
          "oneOf": [
            {
              "description": "A third-party measurement. Carries the measuring agent, so each authorised agent occupies its own slot and concurrent agents never overwrite one another.",
              "type": "object",
              "required": [
                "measured"
              ],
              "properties": {
                "measured": {
                  "type": "object",
                  "required": [
                    "agent",
                    "method"
                  ],
                  "properties": {
                    "agent": {
                      "$ref": "#/definitions/Addr"
                    },
                    "method": {
                      "$ref": "#/definitions/Method"
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            },
            {
              "description": "The subject's own signed declaration, relayed by some agent. Carries no writer component, so a subject has exactly one self-declared slot no matter which agent relayed it; conflicting relays are resolved by `declared_at` monotonicity.",
              "type": "string",
              "enum": [
                "self_declared"
              ]
            },
            {
              "description": "An admin-set value. Names the admin role rather than an address, so transferring the role does not orphan existing overrides.",
              "type": "string",
              "enum": [
                "override"
              ]
            }
          ]
        },
        "SourceEntry": {
          "description": "A `(source, entry)` pair belonging to a single subject.",
          "type": "object",
          "required": [
            "entry",
            "source"
          ],
          "properties": {
            "entry": {
              "$ref": "#/definitions/LocationEntry"
            },
            "source": {
              "$ref": "#/definitions/Source"
            }
          },
          "additionalProperties": false
        },
        "Subject": {
          "description": "The subject an entry describes: its class plus the id whose encoding that class fixes. This typed form is what messages and query responses carry; [`Subject::id_bytes`] is the opaque per-class encoding used in the storage key and the digest leaf.",
          "oneOf": [
            {
              "description": "A bonded nym-node. The id encodes as a big-endian `u32`, so ids order numerically under the store's key ordering and decode back to a [`NodeId`] for the mixnet unbond callback.",
              "type": "object",
              "required": [
                "nym_node"
              ],
              "properties": {
                "nym_node": {
                  "type": "object",
                  "required": [
                    "node_id"
                  ],
                  "properties": {
                    "node_id": {
                      "type": "integer",
                      "format": "uint32",
                      "minimum": 0.0
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            }
          ]
        }
      }
    },
    "subject_entries": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SubjectEntriesResponse",
      "description": "Response for [`crate::QueryMsg::SubjectEntries`] and its siblings: everything held for one subject, in ascending source order.",
      "type": "object",
      "required": [
        "entries",
        "subject"
      ],
      "properties": {
        "entries": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/SourceEntry"
          }
        },
        "subject": {
          "$ref": "#/definitions/Subject"
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "Binary": {
          "description": "Binary is a wrapper around Vec<u8> to add base64 de/serialization with serde. It also adds some helper methods to help encode inline.\n\nThis is only needed as serde-json-{core,wasm} has a horrible encoding for Vec<u8>. See also <https://github.com/CosmWasm/cosmwasm/blob/main/docs/MESSAGE_TYPES.md>.",
          "type": "string"
        },
        "LocationAttestation": {
          "description": "The subject's own attestation over a self-declared location, present only on [`Source::SelfDeclared`] entries.",
          "type": "object",
          "required": [
            "declared_at",
            "signature"
          ],
          "properties": {
            "declared_at": {
              "description": "Unix timestamp, in seconds, at which the subject signed the artifact. Strictly monotonic per subject and bounded above by block time plus `MAX_SKEW`, so a superseded artifact cannot be replayed and a far-future one cannot freeze the slot.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "signature": {
              "description": "The subject's ed25519 signature over the domain-separated signing payload.",
              "allOf": [
                {
                  "$ref": "#/definitions/Binary"
                }
              ]
            }
          },
          "additionalProperties": false
        },
        "LocationEntry": {
          "description": "A stored location entry: the opaque payload, when it reached the chain, and the subject's attestation where one exists.",
          "type": "object",
          "required": [
            "checked_at",
            "payload"
          ],
          "properties": {
            "attestation": {
              "description": "The subject's attestation. Populated on [`Source::SelfDeclared`] entries only; measured and overridden entries carry no signature from anyone.",
              "anyOf": [
                {
                  "$ref": "#/definitions/LocationAttestation"
                },
                {
                  "type": "null"
                }
              ]
            },
            "checked_at": {
              "description": "Unix timestamp, in seconds, taken from the block that wrote this entry. Committed to the digest leaf, so re-submitting an unchanged location changes the digest and a client that verifies the digest also verifies freshness.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "payload": {
              "$ref": "#/definitions/LocationPayload"
            }
          },
          "additionalProperties": false
        },
        "LocationPayload": {
          "description": "The location payload as the contract holds it: a format version plus opaque `content` bytes the contract never parses, validates, normalises or re-serialises.\n\nUnder `version = 1` the `content` bytes are UTF-8 JSON, so a web consumer can base64-decode and `JSON.parse` without obtaining a schema. The version byte sits outside `content` so a later version can change the format itself, not merely the schema.\n\nStoring the bytes verbatim is a correctness invariant rather than an optimisation: a relayed self-declaration's signature is over exactly these bytes, and JSON key ordering, whitespace and floating-point formatting all vary between implementations.",
          "type": "object",
          "required": [
            "content",
            "version"
          ],
          "properties": {
            "content": {
              "description": "The opaque payload bytes, stored and returned exactly as submitted.",
              "allOf": [
                {
                  "$ref": "#/definitions/Binary"
                }
              ]
            },
            "version": {
              "description": "Selects the format of `content`. A version is never reused for another format.",
              "type": "integer",
              "format": "uint8",
              "minimum": 0.0
            }
          },
          "additionalProperties": false
        },
        "Method": {
          "description": "How a measurement was obtained. Closed enum for the same reason [`SubjectClass`] is: it lives in the key, so adding a vendor or a technique is one variant with no leaf-encoding change and no state migration.\n\nRetiring a variant is a migration that must subtract every affected leaf from the accumulator before deleting the entries, and its discriminant must be tombstoned rather than reused.",
          "oneOf": [
            {
              "description": "An ipinfo.io lookup of the subject's resolved addresses.",
              "type": "string",
              "enum": [
                "ip_info"
              ]
            }
          ]
        },
        "Source": {
          "description": "Who asserted an entry, as the final component of its key. A single discriminant rather than separate `kind` and `writer` components, so combinations that would be meaningless are not representable.",
          "oneOf": [
            {
              "description": "A third-party measurement. Carries the measuring agent, so each authorised agent occupies its own slot and concurrent agents never overwrite one another.",
              "type": "object",
              "required": [
                "measured"
              ],
              "properties": {
                "measured": {
                  "type": "object",
                  "required": [
                    "agent",
                    "method"
                  ],
                  "properties": {
                    "agent": {
                      "$ref": "#/definitions/Addr"
                    },
                    "method": {
                      "$ref": "#/definitions/Method"
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            },
            {
              "description": "The subject's own signed declaration, relayed by some agent. Carries no writer component, so a subject has exactly one self-declared slot no matter which agent relayed it; conflicting relays are resolved by `declared_at` monotonicity.",
              "type": "string",
              "enum": [
                "self_declared"
              ]
            },
            {
              "description": "An admin-set value. Names the admin role rather than an address, so transferring the role does not orphan existing overrides.",
              "type": "string",
              "enum": [
                "override"
              ]
            }
          ]
        },
        "SourceEntry": {
          "description": "A `(source, entry)` pair belonging to a single subject.",
          "type": "object",
          "required": [
            "entry",
            "source"
          ],
          "properties": {
            "entry": {
              "$ref": "#/definitions/LocationEntry"
            },
            "source": {
              "$ref": "#/definitions/Source"
            }
          },
          "additionalProperties": false
        },
        "Subject": {
          "description": "The subject an entry describes: its class plus the id whose encoding that class fixes. This typed form is what messages and query responses carry; [`Subject::id_bytes`] is the opaque per-class encoding used in the storage key and the digest leaf.",
          "oneOf": [
            {
              "description": "A bonded nym-node. The id encodes as a big-endian `u32`, so ids order numerically under the store's key ordering and decode back to a [`NodeId`] for the mixnet unbond callback.",
              "type": "object",
              "required": [
                "nym_node"
              ],
              "properties": {
                "nym_node": {
                  "type": "object",
                  "required": [
                    "node_id"
                  ],
                  "properties": {
                    "node_id": {
                      "type": "integer",
                      "format": "uint32",
                      "minimum": 0.0
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            }
          ]
        }
      }
    },
    "subject_measurements": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SubjectEntriesResponse",
      "description": "Response for [`crate::QueryMsg::SubjectEntries`] and its siblings: everything held for one subject, in ascending source order.",
      "type": "object",
      "required": [
        "entries",
        "subject"
      ],
      "properties": {
        "entries": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/SourceEntry"
          }
        },
        "subject": {
          "$ref": "#/definitions/Subject"
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "Binary": {
          "description": "Binary is a wrapper around Vec<u8> to add base64 de/serialization with serde. It also adds some helper methods to help encode inline.\n\nThis is only needed as serde-json-{core,wasm} has a horrible encoding for Vec<u8>. See also <https://github.com/CosmWasm/cosmwasm/blob/main/docs/MESSAGE_TYPES.md>.",
          "type": "string"
        },
        "LocationAttestation": {
          "description": "The subject's own attestation over a self-declared location, present only on [`Source::SelfDeclared`] entries.",
          "type": "object",
          "required": [
            "declared_at",
            "signature"
          ],
          "properties": {
            "declared_at": {
              "description": "Unix timestamp, in seconds, at which the subject signed the artifact. Strictly monotonic per subject and bounded above by block time plus `MAX_SKEW`, so a superseded artifact cannot be replayed and a far-future one cannot freeze the slot.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "signature": {
              "description": "The subject's ed25519 signature over the domain-separated signing payload.",
              "allOf": [
                {
                  "$ref": "#/definitions/Binary"
                }
              ]
            }
          },
          "additionalProperties": false
        },
        "LocationEntry": {
          "description": "A stored location entry: the opaque payload, when it reached the chain, and the subject's attestation where one exists.",
          "type": "object",
          "required": [
            "checked_at",
            "payload"
          ],
          "properties": {
            "attestation": {
              "description": "The subject's attestation. Populated on [`Source::SelfDeclared`] entries only; measured and overridden entries carry no signature from anyone.",
              "anyOf": [
                {
                  "$ref": "#/definitions/LocationAttestation"
                },
                {
                  "type": "null"
                }
              ]
            },
            "checked_at": {
              "description": "Unix timestamp, in seconds, taken from the block that wrote this entry. Committed to the digest leaf, so re-submitting an unchanged location changes the digest and a client that verifies the digest also verifies freshness.",
              "type": "integer",
              "format": "uint64",
              "minimum": 0.0
            },
            "payload": {
              "$ref": "#/definitions/LocationPayload"
            }
          },
          "additionalProperties": false
        },
        "LocationPayload": {
          "description": "The location payload as the contract holds it: a format version plus opaque `content` bytes the contract never parses, validates, normalises or re-serialises.\n\nUnder `version = 1` the `content` bytes are UTF-8 JSON, so a web consumer can base64-decode and `JSON.parse` without obtaining a schema. The version byte sits outside `content` so a later version can change the format itself, not merely the schema.\n\nStoring the bytes verbatim is a correctness invariant rather than an optimisation: a relayed self-declaration's signature is over exactly these bytes, and JSON key ordering, whitespace and floating-point formatting all vary between implementations.",
          "type": "object",
          "required": [
            "content",
            "version"
          ],
          "properties": {
            "content": {
              "description": "The opaque payload bytes, stored and returned exactly as submitted.",
              "allOf": [
                {
                  "$ref": "#/definitions/Binary"
                }
              ]
            },
            "version": {
              "description": "Selects the format of `content`. A version is never reused for another format.",
              "type": "integer",
              "format": "uint8",
              "minimum": 0.0
            }
          },
          "additionalProperties": false
        },
        "Method": {
          "description": "How a measurement was obtained. Closed enum for the same reason [`SubjectClass`] is: it lives in the key, so adding a vendor or a technique is one variant with no leaf-encoding change and no state migration.\n\nRetiring a variant is a migration that must subtract every affected leaf from the accumulator before deleting the entries, and its discriminant must be tombstoned rather than reused.",
          "oneOf": [
            {
              "description": "An ipinfo.io lookup of the subject's resolved addresses.",
              "type": "string",
              "enum": [
                "ip_info"
              ]
            }
          ]
        },
        "Source": {
          "description": "Who asserted an entry, as the final component of its key. A single discriminant rather than separate `kind` and `writer` components, so combinations that would be meaningless are not representable.",
          "oneOf": [
            {
              "description": "A third-party measurement. Carries the measuring agent, so each authorised agent occupies its own slot and concurrent agents never overwrite one another.",
              "type": "object",
              "required": [
                "measured"
              ],
              "properties": {
                "measured": {
                  "type": "object",
                  "required": [
                    "agent",
                    "method"
                  ],
                  "properties": {
                    "agent": {
                      "$ref": "#/definitions/Addr"
                    },
                    "method": {
                      "$ref": "#/definitions/Method"
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            },
            {
              "description": "The subject's own signed declaration, relayed by some agent. Carries no writer component, so a subject has exactly one self-declared slot no matter which agent relayed it; conflicting relays are resolved by `declared_at` monotonicity.",
              "type": "string",
              "enum": [
                "self_declared"
              ]
            },
            {
              "description": "An admin-set value. Names the admin role rather than an address, so transferring the role does not orphan existing overrides.",
              "type": "string",
              "enum": [
                "override"
              ]
            }
          ]
        },
        "SourceEntry": {
          "description": "A `(source, entry)` pair belonging to a single subject.",
          "type": "object",
          "required": [
            "entry",
            "source"
          ],
          "properties": {
            "entry": {
              "$ref": "#/definitions/LocationEntry"
            },
            "source": {
              "$ref": "#/definitions/Source"
            }
          },
          "additionalProperties": false
        },
        "Subject": {
          "description": "The subject an entry describes: its class plus the id whose encoding that class fixes. This typed form is what messages and query responses carry; [`Subject::id_bytes`] is the opaque per-class encoding used in the storage key and the digest leaf.",
          "oneOf": [
            {
              "description": "A bonded nym-node. The id encodes as a big-endian `u32`, so ids order numerically under the store's key ordering and decode back to a [`NodeId`] for the mixnet unbond callback.",
              "type": "object",
              "required": [
                "nym_node"
              ],
              "properties": {
                "nym_node": {
                  "type": "object",
                  "required": [
                    "node_id"
                  ],
                  "properties": {
                    "node_id": {
                      "type": "integer",
                      "format": "uint32",
                      "minimum": 0.0
                    }
                  },
                  "additionalProperties": false
                }
              },
              "additionalProperties": false
            }
          ]
        }
      }
    },
    "whitelist": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "WhitelistResponse",
      "type": "object",
      "required": [
        "agents"
      ],
      "properties": {
        "agents": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/WhitelistEntry"
          }
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "AgentPermissions": {
          "description": "What a whitelisted agent is permitted to write. The flags are independent: an agent may be trusted to measure without being trusted to relay self-declarations, or the reverse.",
          "type": "object",
          "required": [
            "can_measure",
            "can_relay_self_declared"
          ],
          "properties": {
            "can_measure": {
              "description": "May write [`Source::Measured`] entries.",
              "type": "boolean"
            },
            "can_relay_self_declared": {
              "description": "May write [`Source::SelfDeclared`] entries on a subject's behalf.",
              "type": "boolean"
            }
          },
          "additionalProperties": false
        },
        "WhitelistEntry": {
          "description": "One whitelisted agent together with its permissions.",
          "type": "object",
          "required": [
            "agent",
            "permissions"
          ],
          "properties": {
            "agent": {
              "$ref": "#/definitions/Addr"
            },
            "permissions": {
              "$ref": "#/definitions/AgentPermissions"
            }
          },
          "additionalProperties": false
        }
      }
    }
  }
}
