---
- name: "Deploy Nym node"
  hosts: all    # or a specific host/group
  become: true
  roles:
    - base
    - nym
    - nginx
    - tunnel # comment for mixnode
    - quic # comment for mixnode or non-wireguard gateway
